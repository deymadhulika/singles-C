#include <stdio.h>

int main()
{
   int num, f= 1, i=1 ;
   
   printf("\nEnter a number to calculate factorial : ");
   scanf("%d", &num);
   
   while(i<=num)
   {
       f=f*i ;
       i++ ;
   }
   
   printf("\nThe factorial is : %d",f);
    return 0;
}
